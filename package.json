{
  "name": "cushion-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "dev:desktop": "tauri dev",
    "dev:ios": "tauri ios dev",
    "build": "node build-config.js && tauri build",
    "build:test": "TAURI_BUILD_TEST=true node build-config.js && tauri build --debug",
    "build:desktop": "node build-config.js && tauri build",
    "build:ios": "tauri ios build",
    "setup:ios": "tauri ios init",
    "setup:android": "tauri android init",
    "test:deep-links": "open cushion://auth/success?token=test&callbackUrl=%2Fdashboard"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0",
    "@tauri-apps/plugin-notification": "^2.3.1"
  }
}
